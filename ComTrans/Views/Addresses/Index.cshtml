@model IEnumerable<ComTrans.Models.Address>

@{
    ViewBag.Title = "Адреса доставки";
}

<h2>Адреса доставки</h2>

@*<p>
        @Html.ActionLink("Добавить адрес", "Create")
    </p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Address1)
        </th>
        
        @*<th>
        @Html.DisplayNameFor(model => model.Order.Id)
    </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Coord)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Address1)
        </td>
        @*<td>
        @Html.DisplayFor(modelItem => item.Order.Id)
    </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.Coord)
        </td>
        <td>
            @Html.ActionLink("Изменить", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Подробнее", "Details", new { id=item.Id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
